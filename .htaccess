RewriteEngine On

# 1. Define a base para as reescritas
RewriteBase /

# 2. Se o arquivo ou diretório solicitado existir fisicamente, acessa-o diretamente
# Isso garante que imagens, CSS e JS (como em /assets/) carreguem normalmente

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# 3. Redireciona todas as outras solicitações para o index.php
# Isso permite que o PHP leia a URL /produtos/ebook-maternidade
RewriteRule ^(.*)$ index.php [L,QSA]